### Prebuilt caddy-cloudflare image
What it says on the box. Only supports `amd64` for now. Open a PR if you want `arm64` and others.

```
docker pull ghcr.io/minhducsun2002/caddy-cloudflare:2.10.0-alpine
```